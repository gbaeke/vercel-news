Article title: {{ title }}

Article summary: {{ summary }}

Article body:
{{ body }}

Editor instruction: {{ instructions }}
Requested type: {{ diagram_type }}
Requested direction: {{ direction }}
Requested detail: {{ detail }}

Create a single diagram. For simple detail use roughly 3–5 nodes or messages, standard 5–7, and detailed no more than 9. Respect the requested direction when flowchart syntax is used; choose the clearest direction when it is auto. Keep labels to one short line where possible and give the primary story the most direct visual path.
