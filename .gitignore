# deps & build output
node_modules
.next
tsconfig.tsbuildinfo
voice-previews/

# env files: everything except the template and the (secret-free) test config
.env*
!.env.example
!.env.test

# vercel project linkage
.vercel

# local agent tooling (installed by integrations)
.claude/
.agents/
skills-lock.json
